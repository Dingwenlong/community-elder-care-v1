# ESP32 实物验收回执

状态：本轮跳过，未通过。

2026-08-25 本机没有发现可用串口设备。用户确认本轮先跳过，因此没有执行接线检查、烧录、长按 SOS、重复上报、断线重连和服务端回读。固件编译通过与 Web 设备模拟器通过都不能替代实物验收。

| 字段 | 记录 |
|---|---|
| 开发板型号 | 无实物记录 |
| 串口名称 | 不记录 |
| 固件 SHA-256 | 无实物烧录回执 |
| 长按 SOS | 未执行 |
| 重复信号幂等 | 未执行 |
| 断线与重连 | 未执行 |
| 服务端同事件回读 | 未执行 |

补测时应在同一个 PowerShell 进程中设置物理设备令牌，运行物理模式的 PlatformIO 准备与烧录，再启动演示环境。回执不得写入 Wi-Fi 密码、完整设备令牌或其他凭证。
